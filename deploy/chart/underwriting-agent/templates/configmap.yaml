apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "agent.name" . }}-config
  labels:
    {{- include "agent.labels" . | nindent 4 }}
data:
  {{- range $k, $v := .Values.config }}
  {{ $k }}: {{ $v | quote }}
  {{- end }}
